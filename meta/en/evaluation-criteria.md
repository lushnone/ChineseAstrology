# Listing rules (not a leaderboard)

**(Secondary.)** Canonical: [简体中文](../zh-CN/evaluation-criteria.md).

This repo is a **directory**. Fill `name`, `urls`, `features`, factual `summary`, and `last_verified`.

`scores` are optional personal notes; **no official rankings**.

**Include** working, on-topic tools. **Archive** dead links and spam. No “best” or fortune claims in text.
